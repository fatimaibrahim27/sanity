import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["cdn.sanity.io"], // Add 'cdn.sanity.io' here
  },
  // دیگر configuration options یہاں شامل کر سکتے ہیں
};

export default nextConfig;
